<Window x:Class="ResBinManager.Views.FontReplaceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Replace Font Resources"
        Width="700" Height="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 当前字体信息 -->
        <GroupBox Header="Current Font" Grid.Row="0" Margin="0,0,0,10">
            <StackPanel Margin="10">
                <TextBlock x:Name="CurrentFontDataInfo" Text="resfont.bin: Loading..." Margin="0,2"/>
                <TextBlock x:Name="CurrentFontIndexInfo" Text="resfontidx.bin: Loading..." Margin="0,2"/>
            </StackPanel>
        </GroupBox>
        
        <!-- 新文件选择 -->
        <GroupBox Header="New Font Files" Grid.Row="1" Margin="0,0,0,10">
            <StackPanel Margin="10">
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <TextBlock Text="resfont.bin:" Width="100" VerticalAlignment="Center"/>
                    <TextBox x:Name="NewFontDataPath" Width="400" IsReadOnly="True" VerticalContentAlignment="Center" Margin="5,0"/>
                    <Button Content="Browse..." Click="BrowseFontData_Click" Width="80" Margin="5,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <TextBlock Text="resfontidx.bin:" Width="100" VerticalAlignment="Center"/>
                    <TextBox x:Name="NewFontIndexPath" Width="400" IsReadOnly="True" VerticalContentAlignment="Center" Margin="5,0"/>
                    <Button Content="Browse..." Click="BrowseFontIndex_Click" Width="80" Margin="5,0,0,0"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>
        
        <!-- 验证结果 -->
        <GroupBox Header="Validation" Grid.Row="2" Margin="0,0,0,10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ValidationResult" 
                          Text="Please select both font files to validate." 
                          TextWrapping="Wrap"
                          FontFamily="Consolas"
                          FontSize="11"
                          Margin="5"/>
            </ScrollViewer>
        </GroupBox>
        
        <!-- 按钮 -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="3">
            <Button Content="Preview Font" Click="Preview_Click" Margin="0,0,10,0" IsEnabled="False" x:Name="PreviewButton"/>
            <Button Content="Replace Both" Click="Replace_Click" IsEnabled="False" x:Name="ReplaceButton"/>
            <Button Content="Cancel" Click="Cancel_Click" Margin="10,0,0,0" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
