<UserControl x:Class="ThunderSE.Ui.SettingWindow.Awb.ModeSwitchControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="300"
             Background="Transparent">
    
    <UserControl.Resources>
        <!-- 模式切换按钮样式 -->
        <Style x:Key="ModeToggleButtonStyle" TargetType="RadioButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" Padding="6">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" 
                                        Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Tag, FallbackValue=#FF0078D7}"/>
                                <Setter TargetName="border" Property="BorderBrush" 
                                        Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Tag, FallbackValue=#FF0078D7}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter TargetName="border" Property="Background" 
                                        Value="#FFF5F5F5"/>
                                <Setter TargetName="border" Property="BorderBrush" 
                                        Value="#FFCCCCCC"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" 
                                        Value="#FFE8E8E8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="3">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧标签 -->
        <TextBlock Grid.Column="0" Text="🎛️ 操作模式:" VerticalAlignment="Center" 
                   FontSize="11" FontWeight="SemiBold" Foreground="#333333"
                   Margin="0,0,8,0"/>

        <!-- 右侧：模式切换按钮组 -->
        <StackPanel Grid.Column="1" Orientation="Horizontal">
            
            <!-- 传统模式按钮 - 蓝色主题 (与原UI一致) -->
            <RadioButton x:Name="RbTraditionalMode" 
                         Content="📊 传统模式(32点)"
                         GroupName="OperationModeGroup"
                         IsChecked="True"
                         Tag="#FF0078D7"
                         Style="{StaticResource ModeToggleButtonStyle}"
                         ToolTip="使用原始的32点编辑方式，完全手动控制"
                         Checked="RbTraditionalMode_Checked"/>

            <!-- 智能模式按钮 - 绿色主题 (区分新功能) -->
            <RadioButton x:Name="RbSmartMode" 
                         Content="🧠 智能模式(8点)"
                         GroupName="OperationModeGroup"
                         Tag="#FF28A745"
                         Style="{StaticResource ModeToggleButtonStyle}"
                         Margin="2,0,0,0"
                         ToolTip="使用智能插值算法，只需编辑8个关键点"
                         Checked="RbSmartMode_Checked"/>
        </StackPanel>
    </Grid>
</UserControl>
